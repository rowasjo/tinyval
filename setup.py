#!/usr/bin/env python

# This is required to install the package in editable mode.
# If setuptools implements PEP-660 then only pyproject.toml is required.

import setuptools

if __name__ == "__main__":
    setuptools.setup()
